---
description: Add the documented data repositories as submodules and create a worktree for each.
---

Use `$setup` to create this repository's data-engineering workspace. Run its setup script from the repository root and report the created submodules and worktrees. Do not commit the resulting `.gitmodules` or gitlink changes unless explicitly asked.
